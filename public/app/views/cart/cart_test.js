'use strict';

describe('myApp.cart module', function() {

  beforeEach(module('myApp.cart'));

  describe('cart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});