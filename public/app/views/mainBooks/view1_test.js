'use strict';

describe('myApp.mainBooks module', function() {

  beforeEach(module('myApp.mainBooks'));

  describe('mainBooks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});